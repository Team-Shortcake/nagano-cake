<aside id="sub-area">
  <div class="col-24">
      <table class="table table-hover table-bordered">
        <thead>
          <tr>
            <th class="active text-center">
              <h5 class="genre_said">ジャンル検索</h5>
            </th>
          </tr>
        </thead>
        <tbody>
          <% genres.each do |genre| %>
            <tr>
              <!-- each文でジャンル名を取り出し、クリックした際にroutesで設定したitemsのパスを通って、itemsコントローラのsearchアクションに繋げてる。パラメーターにはクリックしたジャンルのidを渡してる。 -->
              <td class="underline", style="text-align: center;">
                <i class="fas fa-caret-right">
                   <%= link_to genre.name, search_items_path(genre), method: :get %>
                </i>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
  </div>
</aside>