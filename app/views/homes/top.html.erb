<div class="container">
  <div class="row">
     <!-- ジャンル検索サイドバー -->
    <%= render "publicsidebar/sidebar", genres: @genres %>
    
    <div class="col-xs-9">
      <div class="background_top">
        <p class="font-text">
          ようこそ、ながのCakeへ！<br>
          このサイトは、ケーキ販売のECサイトになります。<br>
          会員でない方も商品の閲覧はできますが、<br>
          購入には会員登録が必要になります。
        </p>
      </div>
      
      <!-- 新着商品表示 -->
      <br>
      <h4>新着商品</h4>
      <br>
      <div class="row">
        <div id="multipale-items">
          <% @items.each do |item| %>
          <div class="new-items">
            <!-- 画像をクリックしたら商品詳細画面へ遷移 -->
            <%= link_to item_path(item.id) do %>
            <%= attachment_image_tag item, :image, size: "150x150", fallback: "no_image.jpg" %><br>
            <% end %>
            <p><%= item.item_name %></p>
            <!-- 税込計算式/.floor => 浮動小数点数の切り捨てを行うためFloatクラスのfloorメソッドを使用 -->
            <p>¥<%= (item.unit_price_without_tax*1.1).floor %></p>
          </div>
          <% end %>
        </div>  
      </div>
    </div>
  </div>
  
  <div class="text-right">
    <p><%= link_to '商品一覧へ>>', items_path %></p>
  </div>

</div>